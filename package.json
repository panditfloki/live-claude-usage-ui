{
  "name": "claude-usage-meter",
  "displayName": "Mātrā — Claude + Codex Usage",
  "description": "Live Claude and Codex usage — plan limits, tokens and equivalent API cost in one local dashboard.",
  "version": "1.2.3",
  "publisher": "dydxfx",
  "author": {
    "name": "Pandit Floki",
    "email": "pandit@dydxfx.com",
    "url": "https://dydxfx.com"
  },
  "license": "MIT",
  "homepage": "https://dydxfx.com",
  "repository": {
    "type": "git",
    "url": "https://github.com/panditfloki/live-claude-usage-ui.git"
  },
  "bugs": {
    "url": "https://github.com/panditfloki/live-claude-usage-ui/issues",
    "email": "pandit@dydxfx.com"
  },
  "keywords": [
    "claude",
    "claude-code",
    "usage",
    "quota",
    "dashboard",
    "status-bar",
    "anthropic"
  ],
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "commands": [
      {
        "command": "claudeUsage.open",
        "title": "Claude Usage: Open Dashboard"
      },
      {
        "command": "claudeUsage.refresh",
        "title": "Claude Usage: Refresh Now"
      }
    ],
    "configuration": {
      "title": "Claude Usage Meter",
      "properties": {
        "claudeUsage.statusBar.metric": {
          "type": "string",
          "enum": [
            "quota",
            "cost",
            "today",
            "total"
          ],
          "default": "quota",
          "enumDescriptions": [
            "Claude session quota and Codex primary quota, kept separate",
            "Claude current-window cost and Codex primary quota (Codex has no aligned local cost window)",
            "Claude and Codex calendar-today costs, kept separate",
            "Claude and Codex all-time costs, kept separate"
          ],
          "description": "Which figure the status bar shows."
        },
        "claudeUsage.statusBar.show": {
          "type": "boolean",
          "default": true,
          "description": "Show the live burn counter in the status bar."
        }
      }
    }
  },
  "scripts": {
    "dev": "node server.js",
    "package": "vsce package",
    "test": "node --test"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0"
  }
}
